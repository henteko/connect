<div class="page-header">
  <h1><%= @page.title ? @page.title : @page.page_name %> <%= link_to raw('<span class="glyphicon glyphicon-edit h4"></span>'), @page.edit_path %> <%= link_to raw('<span class="glyphicon glyphicon-time h4"></span>'), @page.history_path %></h1>
</div>
