<div class="page-header">
  <h1><%= @blog.title %> <%= link_to raw('<span class="glyphicon glyphicon-edit h4"></span>'), edit_blog_path(@blog, username: @blog.username) if can?(:edit, @blog) %> <%= link_to raw('<span class="glyphicon glyphicon-time h4"></span>'), history_blog_path(@blog, username: @blog.username) %></h1>
</div>
