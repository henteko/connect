<div class="page-header">
  <h1><%= @page.title ? @page.title : @page.url %> <%= link_to raw('<span class="glyphicon glyphicon-edit h4"></span>'), "/pages/#{@page.url}/edit" %> <%= link_to raw('<span class="glyphicon glyphicon-time h4"></span>'), "/pages/#{@page.url}/history" %></h1>
</div>
