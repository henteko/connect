<div class="page-header">
  <h1><%= @page.title ? @page.title : @page.page_name %> <%= link_to raw('<span class="glyphicon glyphicon-edit h4"></span>'), "/pages/#{@page.page_name}/edit" %> <%= link_to raw('<span class="glyphicon glyphicon-time h4"></span>'), "/pages/#{@page.page_name}/history" %></h1>
</div>
